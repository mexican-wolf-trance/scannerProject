#include <stdio.h>
#include <stdlib.h>
#include <unistd.h>
#include <string.h>

#define CHARMAX 1024

int main(int argc, char *argv[])
{
	char pPos;

        if (argc > 2)
        {
                printf("See readme for usage details\nThere shouldn't be more than two argument$
                return 0;
        }

